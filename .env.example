# Root Environment Notes

This root `.env.example` is for high-level notes only.
Project-specific env files:

- Backend: `backend/.env`
- Frontend: `frontend/.env`

Backend keys to configure:
- `APP_URL`
- `DB_*`
- auth/session/token settings
- CORS allowed origins

Frontend keys to configure:
- `VITE_API_URL`
